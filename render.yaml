services:
  - type: worker # Правильно для бота!
    name: my-telegram-bot-worker
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py # Ваш бот запускается этой командой